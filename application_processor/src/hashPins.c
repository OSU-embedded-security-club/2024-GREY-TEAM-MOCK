#include <tinycrypt/sha256.h>
#include <tinycrypt/constants.h>
#include <tinycrypt/utils.h>
#include <stdio.h>

sha256(const uint8_t *plaintext, uint8_t *hashtest) {
    tinycrypt 
    ...
}

int main (void) {
    FILE *file = fopen("./inc/ectf_params.h", "r");
    if (file == NULL) {
        printf("Failed to open global_secrets.h for writing\n");
        return 1;
    }
    fscanf(file, current);
    fscanf(file, next);
    if (current == AP_PIN) {
        current = []
        sha(next, )
        fprintf();
    }
}